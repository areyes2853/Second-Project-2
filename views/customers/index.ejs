
  <!-- views/fruits/index.ejs -->
  <%- include('../partials/header') %>

<body>
  

<h1>All Customers</h1>
<a href="/customers/new">Add New Customer</a>

<% if (customers && customers.length > 0) { %>
  <ul>
    <% customers.forEach(customer => { %>
      <li>
        <a href="/customers/<%= customer._id %>">
          <%= customer.name %> (<%= customer.email %>)
        </a>
         - <a href="/customers/<%= customer._id %>/edit">Edit</a>
         - <form action="/customers/<%= customer._id %>?_method=DELETE" method="POST" style="display: inline;">
             <button type="submit">Delete</button>
           </form>
      </li>
    <% }) %>
  </ul>
<% } else { %>
  <p>No customers found.</p>
<% } %>

<%- include('../partials/footer') %>
  
  
</html>
